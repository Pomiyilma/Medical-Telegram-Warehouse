version: 2

sources:
  - name: raw_source
    schema: raw
    description: "This source contains the direct, untouched raw data feeds loaded from our Telegram scraping lake."
    tables:
      - name: telegram_messages
        description: "The primary landing table holding raw extracted messages and unstructured media paths from medical channels."
      - name: yolo_detections
        description: "The computer vision categorization outputs generated via YOLOv8 model sweeps."
        
        